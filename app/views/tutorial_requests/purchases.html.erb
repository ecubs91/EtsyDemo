<div class="centre">
  <h2>My Student History</h2>
</div>

<table class="table table-striped table-bordered">
  <tr>
    <th class="center">Tutor</th>
    <th class="center">Subject</th>
    <th class="center">Level</th>
    <th class="center">Duration</th>

  </tr>


  <% @tutorial_requests.each do |tutorial_request| %>
    <tr>
      <td><%= tutorial_request.tutor.name %></td>
      <td><%= tutorial_request.subject %></td>
      <td><%= tutorial_request.level %></td>
      <td><%= tutorial_request.duration %></td>
      <td><%= tutorial_request.created_at.strftime("%B %-d, %Y") %></td>     
    </tr>
  <% end %>
</table>

<br>

<% if user_signed_in? %>
  <%= link_to 'New Listing', new_listing_path, class: "btn btn-link" %>
<% end %>