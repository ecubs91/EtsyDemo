<%= form_for(@disussion_forum) do |f| %>
  <% if @disussion_forum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@disussion_forum.errors.count, "error") %> prohibited this disussion_forum from being saved:</h2>

      <ul>
      <% @disussion_forum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :subject %><br>
    <%= f.text_field :subject %>
  </div>
  <div class="field">
    <%= f.label :question %><br>
    <%= f.text_area :question %>
  </div>
  <div class="field">
    <%= f.label :details %><br>
    <%= f.text_area :details %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
